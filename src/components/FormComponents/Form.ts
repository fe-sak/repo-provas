import styled from 'styled-components';

const Form = styled.form`
  display: flex;
  flex-direction: column;
  gap: 13px;

  p {
    color: #ffa3a3;
    font-weight: 700;
  }
`;

export default Form;
